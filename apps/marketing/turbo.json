{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.local",
        ".env.production",
        ".env.production.local"
      ],
      "env": ["GIT_COMMIT_SHA", "PUBLIC_TURNSTILE_SITE_KEY", "PUBLIC_API_URL"]
    },
    "dev": {
      "env": ["GIT_COMMIT_SHA", "PUBLIC_TURNSTILE_SITE_KEY", "PUBLIC_API_URL"]
    }
  }
}
